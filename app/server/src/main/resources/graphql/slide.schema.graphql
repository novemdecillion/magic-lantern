type Chapter {
  title: String!
}

type SlideConfig {
  title: String!
  chapters: [Chapter!]
}

type Slide {
  slideId: ID!
  enable: Boolean!
  config: SlideConfig!
}

input AddSlideCommand {
  slideId: ID!
  slideFile: Upload!
}