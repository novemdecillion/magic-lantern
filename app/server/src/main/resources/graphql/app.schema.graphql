scalar DateTime
scalar Date
scalar Time

type Query {
  currentUser: User!
  users: [User!]!
  userCount: Int!
  realms: [Realm!]!

  slides: [SlideConfig!]!
  courses: [Course!]!
  groups: [Group!]!
}

type Mutation {
  syncRealm(realmId: String): Boolean
}