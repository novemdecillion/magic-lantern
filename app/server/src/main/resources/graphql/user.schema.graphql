type Authority {
  groupId: ID!
  role: String!
}

type User {
  userId: ID!
  userName: String!
  realmId: String
  enabled: Boolean
  authorities: [String!]!
}

type Realm {
  realmId: String!
  realmName: String
  syncAt: LocalDateTime
}