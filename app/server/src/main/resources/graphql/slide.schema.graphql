type Chapter {
  title: String!
}

type SlideConfig {
  title: String!
  chapters: [Chapter!]
}

type Slide {
  slideId: ID!
  config: SlideConfig!
}

input AddSlideCommand {
  slideId: ID!
  slideFile: Upload!
}